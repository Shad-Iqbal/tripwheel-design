# Commands

Custom slash commands for this project.

**Examples:**
- `new-screen.md` — `/new-screen` creates a new screen file from a template
- `token-audit.md` — `/token-audit` checks for hardcoded values
