# Rules

Place convention files here. Each file loads only when Claude touches relevant files.

**Naming examples:**
- `components.md` — rules for working in `src/components/`
- `tokens.md` — rules for editing `design-tokens.json`
- `naming.md` — naming conventions across the project
