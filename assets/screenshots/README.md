# Version log screenshots

Headless captures at 1440px for entries in `index.html`.

Naming: `{version}-{screen}.png` — e.g. `v042-dashboard-active.png`, `redesign-social.png`.

Add new shots here when shipping a prototype version.
